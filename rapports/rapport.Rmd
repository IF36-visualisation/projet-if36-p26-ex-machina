---
title: "Rapport d’analyse exploratoire — Croustillant Menu"
author: "Jules TIMADJI, Ange Ariane KAMGUE MAGNE, Housséni YABRE, Rayan LEGRAND"
date: "2026-04-20"
output:
  html_document:
    toc: true
    df_print: paged
  pdf_document:
    toc: true
    number_sections: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(tibble)
library(ggplot2)
library(tidyr)
library(readr)
library(forcats)
library(scales)
``` 

# Introduction

Ce projet s’inscrit dans le domaine de l’analyse de données appliquée à la restauration universitaire CROUS. Il repose sur l’exploitation des données fournies par l’API **Croustillant Menu** (https://api.croustillant.menu/) qui permet de récupérer les menus des restaurants ainsi que les détails des plats proposés.

Le projet a pour objectif de réaliser une **analyse exploratoire** des menus et des restaurants afin de :

- Identifier les tendances dans l’offre culinaire selon les restaurants et les périodes (ex. février 2026)  
- Analyser la diversité des plats et des catégories (Entrées, Plats, Desserts)  
- Comparer les menus entre restaurants et observer des régularités ou anomalies  
- Détecter des insights exploitables pour des analyses plus avancées (ex. popularité des plats, types de repas les plus fréquents) 


## Présentation des données

Le jeu de données comporte plusieurs fichiers CSV situés dans le dossier `data/` :

### 1. **`liste_restaurants.csv`** : liste des restaurants distincts
- **Nombre d’observations :** 942 (correspond au nombre de restaurants présents dans le fichier CSV)  
- **Nombre de variables :** 20  

| Champ | Type | Description |
|-------|------|-------------|
| `code` | entier | Identifiant unique du restaurant |
| `nom` | texte | Nom du restaurant |
| `adresse` | texte | Adresse complète |
| `latitude` | numérique | Latitude GPS |
| `longitude` | numérique | Longitude GPS |
| `horaires` | texte | Horaires d’ouverture (ex. 11:00-22:00) |
| `jours_ouvert` | texte | Jours de la semaine où le restaurant est ouvert |
| `image_url` | texte | Lien vers une image représentative du restaurant |
| `email` | texte | Adresse email du restaurant |
| `telephone` | texte | Numéro de téléphone |
| `ispmr` | booléen | Accessibilité PMR (personnes à mobilité réduite) |
| `zone` | texte | Zone géographique ou quartier |
| `paiement` | texte | Modes de paiement acceptés |
| `acces` | texte | Informations sur l’accès (parking, transports…) |
| `ouvert` | booléen | Indique si le restaurant est actuellement ouvert |
| `actif` | booléen | Indique si le restaurant est actif dans la base |
| `region.code` | entier | Code de la région |
| `region.libelle` | texte | Nom de la région |
| `type.code` | entier | Code du type de restaurant |
| `type.libelle` | texte | Libellé du type de restaurant (ex. Brasserie, Fast-food) |
   - Chaque observation correspond à un restaurant unique dans une région.

### 2. **`menus_complets_enrichis.csv`** : liste des  menus pour chaque restaurant

### Description
Cette table contient l’ensemble des menus proposés dans les restaurants universitaires.  
Chaque ligne correspond à un plat spécifique servi dans un restaurant donné, à une date, pour un type de repas et une catégorie précis.

Les données ont été enrichies manuellement à partir d’informations issues du site **OpenFoodFacts** (https://world-fr.openfoodfacts.org/), puis combinées avec les données récupérées via l’API **CROUStillant Menu**.

Ce choix d’enrichissement manuel a été motivé par plusieurs contraintes :
- le volume très important des données OpenFoodFacts (~9 Go),
- la complexité de leur structure,
- la forte sensibilité aux variations d’écriture des noms de plats.

- **Nombre d’observations :** 80 307 
- **Nombre de variables :** 10

### Colonnes

| Champ | Type | Description |
|----------|------|------------|
| `restaurant_id` | entier | Identifiant du restaurant |
| `date` | date | Date du menu |
| `repas` | texte | Type de repas |
| `categorie` | texte | Catégorie du plat |
| `plat` | texte | Nom du plat |
| `nutriscore` | texte | Score nutritionnel (A–E) |
| `regime` | texte | Type alimentaire (carné, végétarien, etc) |
| `impact_carbone` | texte | Empreinte carbone (bas, moyen, élevé) |
| `style_culinaire` | texte | Style du plat (soupe, cuisine traditionnelle, etc) |
| `calories_estimees` | entier | Nombre de calories estimées en kcal |

**Organisation et sous-groupes :**  

- Les données sont hiérarchisées : **restaurants → menus → types de repas → catégories → plats**  
- Chaque restaurant peut avoir plusieurs menus par jour et plusieurs plats par catégorie  

**Format :** CSV, séparateur `,`  

**Contexte et choix des données :**  

- Ces données permettent d’étudier les menus sur différentes périodes et restaurants.  
- Elles sont pertinentes pour analyser la diversité culinaire, la récurrence de certains plats, ou les préférences selon le type de repas et les catégories de plats.  


<br>
<hr>
<br>

# Rendu 1

<br>
<hr>
<br>

## Question 1 : Quel jour du mois concentre le plus de menus ?

Cette question cherche à identifier si certains jours du mois sont plus actifs que d'autres dans l'offre de restauration CROUS. Nous nous attendons à observer une concentration plus forte en début de mois, liée à l'organisation calendaire des semaines scolaires, et un effondrement en fin de semaine (samedi/dimanche).

Les données utilisées sont : `date` (donnée temporelle, quantitative discrète une fois transformée en jour du mois) et le **nombre de lignes de menus** (quantitative continue agrégée).

#### **Méthodologie et nettoyage**

Les dates ont été converties au format `Date` via `lubridate`. Nous avons extrait le jour du mois (`day(date)`) afin de comptabiliser le nombre total de lignes de menus pour chaque valeur de 1 à 31. Cette agrégation permet de comparer l'activité selon la position dans le mois, indépendamment du mois calendaire exact.

À noter que certains jours du mois n'apparaissent qu'une seule fois dans les données (sur un seul mois), tandis que d'autres apparaissent deux fois (sur deux mois différents). Le volume brut de menus par jour du mois est donc influencé par ce biais de répétition. Pour corriger cela, nous normalisons le compte par le nombre de jours effectivement observés pour chaque valeur.

#### **Visualisation et analyse**

```{r q1-menus-par-jour, echo=FALSE, message=FALSE, warning=FALSE, fig.width=12, fig.height=5, out.width="100%"}
library(dplyr)
library(ggplot2)
library(lubridate)
library(readr)
library(scales)

menus <- read_csv("../data/menus_complets_enrichis.csv", show_col_types = FALSE) %>%
  mutate(date = dmy(date))

# Nombre de jours observés par valeur de jour du mois (pour normaliser)
nb_jours_observes <- menus %>%
  distinct(date) %>%
  mutate(jour_mois = day(date)) %>%
  count(jour_mois, name = "nb_jours_obs")

# Agrégation : total de menus par jour du mois
menus_par_jour <- menus %>%
  mutate(jour_mois = day(date)) %>%
  count(jour_mois, name = "nb_menus") %>%
  left_join(nb_jours_observes, by = "jour_mois") %>%
  mutate(
    menus_normalises = nb_menus / nb_jours_obs,
    highlight = menus_normalises == max(menus_normalises)
  )

ggplot(menus_par_jour, aes(x = factor(jour_mois), y = menus_normalises, fill = highlight)) +
  geom_col(show.legend = FALSE) +
  scale_fill_manual(values = c("FALSE" = "#7ab8d4", "TRUE" = "#1a6fa8")) +
  scale_y_continuous(labels = comma_format(big.mark = " ")) +
  labs(
    title    = "Nombre moyen de menus par jour du mois",
    subtitle = "Normalisé par le nombre de fois où chaque jour du mois apparaît dans les données",
    x        = "Jour du mois",
    y        = "Nombre moyen de menus"
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title    = element_text(face = "bold", size = 14),
    panel.grid.major.x = element_blank()
  )
```

### Commentaire

Le graphique montre que l'activité est relativement homogène en semaine tout au long du mois, avec une chute nette sur les jours tombant un week-end (valeurs faibles pour certains 4, 11, 18, 25…). Le jour du mois concentrant le plus de menus en valeur normalisée correspond aux **jours de semaine actifs en début ou mi-mois**, qui coïncident avec les semaines d'enseignement pleines.

### Critique

- L'analyse par jour du mois **mélange des jours de semaine différents selon les mois** : le 1er avril est un mercredi mais le 1er mai un vendredi.
- Une approche complémentaire serait d'analyser l'activité par **jour de la semaine** (lundi–dimanche), qui est davantage structurante pour l'activité CROUS.
- Les données couvrant seulement mars–mai 2026, les conclusions ne sont pas généralisables à l'année entière.

---

## Question 2 : Existe-t-il une saisonnalité dans l'offre des plats ?

Cette question explore si l'offre culinaire évolue dans le temps, notamment selon le mois ou la semaine. Dans le contexte universitaire, on peut s'attendre à une baisse de l'activité pendant les vacances scolaires (semaine de Pâques en avril, début mai avec les ponts) et une offre plus stable en période de cours.

Les données utilisées sont : `date` (temporelle), `categorie` (nominale) et le **nombre de plats** (quantitative agrégée).

#### **Méthodologie et nettoyage**

Nous agrégerons les données à deux niveaux :

1. **Par semaine calendaire** : pour visualiser les grandes tendances temporelles du volume de l'offre.
2. **Par semaine et par catégorie principale** : pour détecter si certaines catégories de plats varient plus que d'autres dans le temps.

Pour simplifier la lisibilité, les catégories ont été regroupées en grandes familles (`Plats`, `Desserts`, `Entrées/Légumes`, `Autre`) en raison de la grande fragmentation des libellés dans les données (384 catégories distinctes).

#### **Visualisation et analyse**

```{r q2-saisonnalite, echo=FALSE, message=FALSE, warning=FALSE, fig.width=13, fig.height=6, out.width="100%"}
library(dplyr)
library(ggplot2)
library(lubridate)
library(readr)
library(scales)
library(stringr)

menus <- read_csv("../data/menus_complets_enrichis.csv", show_col_types = FALSE) %>%
  mutate(date = dmy(date))

# Regroupement des catégories en grandes familles
menus <- menus %>%
  mutate(
    categorie_groupe = case_when(
      str_detect(str_to_lower(categorie), "dessert|pâtisserie|patisserie|fruit|yaourt|fromage") ~ "Desserts",
      str_detect(str_to_lower(categorie), "entree|entrée|légume|legume|salade|soupe|potage")   ~ "Entrées / Légumes",
      str_detect(str_to_lower(categorie), "plat|viande|poisson|porc|bœuf|boeuf|poulet|pizza|pasta|burger") ~ "Plats",
      TRUE                                                                                        ~ "Autre"
    ),
    semaine_debut = floor_date(date, unit = "week", week_start = 1)
  )

# Agrégation par semaine et catégorie
evolution <- menus %>%
  count(semaine_debut, categorie_groupe, name = "nb_plats")

ggplot(evolution, aes(x = semaine_debut, y = nb_plats, color = categorie_groupe, group = categorie_groupe)) +
  geom_line(linewidth = 1.1) +
  geom_point(size = 2.2) +
  scale_x_date(date_labels = "%d %b", date_breaks = "1 week") +
  scale_y_continuous(labels = comma_format(big.mark = " ")) +
  scale_color_manual(
    values = c(
      "Plats"            = "#1a6fa8",
      "Desserts"         = "#e07b39",
      "Entrées / Légumes" = "#3aaa6e",
      "Autre"            = "#b0b0b0"
    )
  ) +
  labs(
    title    = "Évolution hebdomadaire du nombre de plats par grande catégorie",
    subtitle = "Semaines du 23 mars au 23 mai 2026 — restaurants CROUS",
    x        = "Semaine (début)",
    y        = "Nombre de plats proposés",
    color    = "Catégorie"
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title     = element_text(face = "bold", size = 14),
    legend.position = "bottom",
    axis.text.x    = element_text(angle = 35, hjust = 1)
  )
```

### Commentaire

On observe une **chute marquée de l'offre à partir de la semaine du 6 avril**, correspondant aux vacances de Pâques, suivie d'une reprise progressive. La catégorie **Plats** domine l'offre tout au long de la période et est la plus sensible aux variations calendaires. Les **Desserts** et **Entrées/Légumes** suivent la même tendance mais avec une amplitude plus faible. La catégorie **Autre** (offres non classifiables) reste marginale et stable.

### Critique

- Le regroupement en quatre grandes catégories est une simplification : les 384 libellés distincts ne se prêtent pas tous à une classification évidente, et certains cas limites ont pu être mal classés.
- L'analyse porte sur moins de deux mois : on ne peut pas conclure à une saisonnalité annuelle, seulement à des **effets de calendrier universitaire** à court terme.
- La semaine de fin de mars (27/03) est incomplète, ce qui sous-estime artificiellement le point de départ.

---

## Question 5 : Quel est le Nutri-Score moyen des 100 plats les plus fréquents ?

Nous nous intéressons à la qualité nutritionnelle de l'offre de restauration collective CROUS. Le CROUS constitue la principale solution de restauration pour la population étudiante, offrant des repas à tarif social.

L'objectif de cette section est de déterminer si les plats les plus fréquemment proposés (et donc potentiellement les plus consommés) présentent un profil nutritionnel favorable. Nous pouvons supposer que l'offre est dominée par des produits de type "snacking" ou "confort" : plats à base de pains (sandwiches, paninis), de viandes, de fromages, de légumes, ainsi que des pâtisseries. Ces catégories sont souvent associées à des apports caloriques élevés, et nous cherchons à vérifier si cette intuition se traduit par des Nutri-Scores moins favorables (D ou E).

Le Nutri-Score est un label qui facilite l’identification des aliments de bonne qualité nutritionnelle en les notant de A à E. Le meilleur Nutri-Score pour un aliment est le Score A. C'est le graal nutritionnel car il indique que l'aliment présente la meilleure qualité nutritionnelle de sa catégorie. Attention toutefois, cela ne signifie pas qu'il faut manger uniquement des produits classés A, mais qu'ils devraient constituer la base de l'équilibre alimentaire.


#### **Méthodologie et Nettoyage**

Pour répondre à cette question, nous avons choisi une visualisation sous la forme d'un **Lollipop chart**. Ce type de graphique permet de comparer efficacement les moyennes de Nutri-scores pour chaque plat tout en conservant une grande clarté visuelle. Les données utilisées proviennent de notre dataset `liste_menus_enrichis.csv`.


Le jeu de données initial contient de nombreuses entrées textuelles qui ne sont pas des plats (consignes sanitaires, messages marketing, informations de service). Un nettoyage rigoureux a été effectué pour :

- **Exclure les messages de gestion :** ("Simple & gourmand", "Téléchargez l'application").

- **Regrouper les variantes de plats :** (fusionner tous les types de burgers, de pâtes ou de sandwiches). Il existe une grande variété de patisseries, sandwichs, pâtes,etc, qui risqueraient de fausser notre analyse.

- **Numériser le Nutri-Score :** Nous avons converti le Nutri-Score alphabétique (A-E) en échelle numérique (1-5) pour permettre le calcul d'une moyenne en fonction des regroupements effectués.

Les données utilisées sont : `plat` (ce sont des données nominales), `nutriscore` (ce sont des données nominales mais une fois converties, elles devient des données quantitatives / numériques ordinales et discrètes de 1 à 5).


#### **Visualisation et analyse**

Le graphique ci-dessous utilise un format **"Lollipop" divergent**. 

La ligne pointillée centrale représente le Nutri-Score C (valeur 3). Nous avons choisi cette valeur car il s'agit de la médiane des Nutri-scores. 
Visuellement, cela permet également de créer un graphique divergent : tout ce qui part à gauche (vers 1 et 2) est statistiquement "au-dessus de la moyenne théorique", et tout ce qui part à droite (4 et 5) est "en-dessous". Cela nous permet donc de distinguer d'un coup d'œil les plats "plutôt sains" des plats "plutôt gourmands". 
Enfin, C est le pivot : ce sont des aliments dont la qualité nutritionnelle est jugée "moyenne" ou "neutre".

Les plats s'étendant vers la gauche (vert) ont un score moyen plus sain, tandis que ceux s'étendant vers la droite (rouge) présentent un score plus élevé (moins favorable). Ce choix de couleurs s'aligne également avec le mode de labelisation du nutriscore dans les grandes surfaces.

Par souci de lisibilité, nous avons également choisi de placer les plats en abcisses et les valeurs numériques de nutri-score en ordonnées

```{r q5-lollipop_chart, fig.cap="le Nutri-Score moyen des 100 plats les plus fréquents", echo=FALSE, message=FALSE, warning=FALSE, fig.width=16, fig.height=9, out.width="100%"}
#Question 5 : Quel est le Nutri-Score moyen des 100 plats les plus fréquents ?

library(tidyverse)
library(ggplot2)

menus <- read_csv("../data/menus_complets_enrichis.csv")

data_nutri <- menus %>%
  mutate(
    plat_test = iconv(str_to_lower(plat), to = "ASCII//TRANSLIT"),
    plat_test = str_squish(plat_test),
    # Conversion du Nutri-Score
    nutri_num = case_when(
      nutriscore == "A" ~ 1,
      nutriscore == "B" ~ 2,
      nutriscore == "C" ~ 3,
      nutriscore == "D" ~ 4,
      nutriscore == "E" ~ 5,
      TRUE ~ NA_real_
    )
  ) %>%
  # --- BLOC DE NETTOYAGE RADICAL ---
  filter(
    !str_detect(plat_test, "montreal|a emporter|telechargez|note douce|carte detaillee|inspiration du jour|simple & gourmand|manque plus que vous"),
    !str_detect(plat_test, "bon appetit|allergenes|consultez la carte|plat du jour|le dandy|les ameliores|hors d'oeuvre varies"),
    !str_detect(plat_test, "pain est compris|origine france|selon fabrication")
  ) %>%
  # Bloc de regroupement
  mutate(plat_general = case_when(
    str_detect(plat_test, "sandwich|baguette|complet|viennois|ciabatta|bagnat|jambon beurre|rosette|thon mayo|pain speciaux|pain suedois") ~ "Sandwichs & Baguettes",
    str_detect(plat_test, "burger|hamburger|tenders|box tenders|tacos|bacon butternut|chicken hawai") ~ "Burgers, Tenders & Tacos",
    str_detect(plat_test, "panini") ~ "Paninis",
    str_detect(plat_test, "wrap|nordiques") ~ "Wraps & Spécialités froides",
    str_detect(plat_test, "salade|cesar|santorin|bowl|crudites|nicoise|taboule|surimi|coleslaw|oeufs mayo") ~ "Salades & Entrées",
    str_detect(plat_test, "pasta|pate|bolognaise|carbonara|napolitaine|pesto|sodebo|fusilli|penne|coquillette|macaroni|torsade") ~ "Pâtes & Pasta Box",
    str_detect(plat_test, "pizza|margherita|bruchetta") ~ "Pizzas & Bruschettas",
    str_detect(plat_test, "croque[- ]monsieur|croques monsieur") ~ "Croque-monsieur",
    str_detect(plat_test, "quiche|tarte salee|lorraine|chevre|courgette") ~ "Quiches & Tartes salées",
    str_detect(plat_test, "poulet|tikka|curry|cajun|teriyaki|nuggets|escalope|cordon bleu|dinde|carbonade|viande|boeuf|chili con carne") ~ "Plats de Viandes (Rouge & Volaille)",
    str_detect(plat_test, "poisson|saumon|bordelaise|colin|macha ko tharkari") ~ "Plats de Poisson & Exotiques",
    str_detect(plat_test, "yaourt|laitage|fromage blanc|entremets|verre de lait") ~ "Yaourts & Produits Laitiers",
    str_detect(plat_test, "cookie|muffin|beignet|donut|brownie|patisserie|viennoiserie|croissant|pain au chocolat|choco suisse|palmier|gateau|crepe|nutella|biscuit|tartelette") ~ "Viennoiseries & Pâtisseries",
    str_detect(plat_test, "fruit|compote|pomme|banane|orange|confiture") ~ "Fruits & Compotes",
    str_detect(plat_test, "eau |fontaine|cristaline|vittel|volvic|perrier|gazeuse|plate") ~ "Eaux & Fontaines",
    str_detect(plat_test, "coca|soda|red bull|monster|oasis|fuzz|minute maid|jus de|the aromatise|the nature|canette") ~ "Boissons Sucrées (Sodas/Jus/Thés)",
    str_detect(plat_test, "cafe|expresso|latte|macchiato|capuccino|moccaccino|chocolat chaud") ~ "Boissons Chaudes",
    TRUE ~ plat 
  ))

# 2. Calcul du Top 100 et de leur Nutri-Score moyen
top_100_nutri <- data_nutri %>%
  group_by(plat_general) %>%
  summarise(
    n = n(),
    nutri_moyen = mean(nutri_num, na.rm = TRUE)
  ) %>%
  filter(!is.na(nutri_moyen)) %>%
  # Suppression manuelle des dernières miettes si nécessaire
  filter(!plat_general %in% c("Frites", "Chips, pringles", "Chips natures ou aromatisées")) %>%
  arrange(desc(n)) %>%
  head(100)

# 3. Préparation pour le graphique
top_100_nutri_div <- top_100_nutri %>%
  mutate(nutri_label = round(nutri_moyen, 1)) %>%
  arrange(nutri_moyen)


# Visualisation
# Création du graphique "Divergent Lollipop"
# Visualisation
p_vertical <- ggplot(top_100_nutri_div, aes(x = reorder(plat_general, nutri_moyen), y = nutri_moyen)) +
  geom_hline(yintercept = 3, linetype = "dashed", color = "grey80") +
  
  geom_segment(aes(xend = plat_general, y = 3, yend = nutri_moyen), 
               color = "grey85", size = 1) + # Tige légèrement plus épaisse
  
  geom_point(aes(color = nutri_moyen), size = 8) + # Bulles plus grandes (de 5 à 8)
  
  # 1. Augmentation du texte DANS les bulles (size de 1.5 à 3.5)
  geom_text(aes(label = nutri_label), color = "white", size = 3.5, fontface = "bold") +
  
  scale_color_gradientn(
    colors = c("#1a9850", "#91cf60", "#fee08b", "#fc8d59", "#d73027"),
    limits = c(1, 5), 
    breaks = c(1, 2, 3, 4, 5),             
    labels = c("1-A", "2-B", "3-C", "4-D", "5-E"), 
    name = "Qualité Nutritionnelle"
  ) +
  
  theme_minimal() +
  labs(
    title = "5. Nutri-Score moyen des 100 plats les plus fréquents",
    x = "Plats distribués", 
    y = "Moyenne Nutri-Score",
    caption = "Visualisation par : Ange Kamgue | Groupe Ex-Machina"
  ) +
  theme(
    # 2. Augmentation des noms des plats en bas (size de 6 à 10 + gras)
    axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1, size = 10, face = "bold"),
    
    # 3. Augmentation des titres des axes et du graphique
    axis.title = element_text(size = 14, face = "bold"),
    plot.title = element_text(size = 18, face = "bold"),
    
    panel.grid.major.x = element_blank(),
    legend.position = "bottom",
    
    # 4. Augmentation de la taille du texte de la légende
    legend.key.width = unit(3, "cm"),
    legend.title = element_text(size = 12, face = "bold"),
    legend.text = element_text(size = 11)
  )

# Affichage (Assure-toi que ton chunk a fig.width=20 ou plus)
print(p_vertical)

```
 

#### **Interprétation des résultats**
À la lecture de ce graphique, nous observons que :

- Les plats équilibrés : Les soupes, salades composées et plats à base de légumineuses se concentrent sous la barre des 3, affichant des scores A et B.

- Le cœur de l'offre : Une grande partie des plats cuisinés (viandes blanches, pâtes) oscille autour du pivot C.

- Les options "plaisir" : Les burgers, pizzas et surtout les viennoiseries s'élèvent vers les scores 4 (D) et 5 (E), ce qui confirme notre hypothèse initiale sur la densité calorique de l'offre snacking.

<br>
<hr>
<br>

## Question 7 : Comment les catégories varient-elles selon les repas ?

Les restaurants CROUS proposent différents types de repas (matin, midi, soir). On se demande si la structure des catégories de plats (Entrées, Plats, Desserts…) est la même pour chaque repas, ou si elle varie. On s'attend à ce que le midi soit le repas le plus complet, avec toutes les catégories représentées, et le matin le plus limité.

```{r q6-stacked-bar, fig.cap="Répartition des catégories de plats par type de repas", echo=FALSE, message=FALSE, warning=FALSE, fig.width=16, fig.height=9, out.width="100%"}
# Dictionnaire de regroupement des catégories
groupes <- c(
  "Entrées"        = "entr",
  "Plats"          = "plat|chaud|accompagne|grillade",
  "Desserts"       = "dessert|douceur|patisserie|compote|yaourt",
  "Boissons"       = "boisson|cafe|froid",
  "Snacking"       = "sandwich|panini|snacking|salade",
  "Formules/Menus" = "formule|menu",
  "Petit-déjeuner" = "viennois|petit.dej"
)

# Fonction de regroupement
classer <- function(categorie) {
  cat_lower <- tolower(categorie)
  for (groupe in names(groupes)) {
    if (str_detect(cat_lower, groupes[groupe])) return(groupe)
  }
  return("Autre")
}

# Calcul des proportions
structure_repas <- menus %>%
  mutate(categorie_groupe = map_chr(categorie, classer)) %>%
  group_by(repas, categorie_groupe) %>%
  summarise(n = n(), .groups = "drop") %>%
  group_by(repas) %>%
  mutate(proportion = n / sum(n))

# Visualisation
ggplot(structure_repas,
       aes(x = repas, y = proportion, fill = categorie_groupe)) +
  geom_col(position = "fill", width = 0.6) +
  scale_y_continuous(labels = scales::percent_format()) +
  scale_fill_brewer(palette = "Set2", name = "Catégorie") +
  labs(
    title = "Répartition des catégories de plats par type de repas",
    x     = "Type de repas",
    y     = "Proportion"
  ) +
  theme_minimal(base_size = 11)
```

#### **Interprétation**

Le graphique révèle des structures très différentes selon le type de repas, ce qui confirme notre hypothèse de départ.

-   **Le matin** est dominé par les formules et/ou menus (\~25%), le Petit-déjeuner et les Boissons — cohérent avec une offre de restauration rapide matinale.

-   **Le midi** est le repas le plus diversifié : on y trouve du Snacking (\~30%), des Formules/Menus, des Plats, des Entrées et des Desserts — toutes les catégories sont représentées.

-   **Le soir** ressemble au midi mais avec une part de "Autre" très importante (\~30%), ce qui suggère que les restaurants du soir utilisent des catégories non standardisées, difficiles à classer.

La catégorie "Autre" reste présente dans les trois repas, ce qui confirme le problème de normalisation des catégories observé précédemment. Ainsi une partie significative des données reste inclassable avec notre regroupement actuel.

**Nouvelle question :** La forte présence des Formules/Menus au matin et au midi cache-t-elle une vraie diversité de plats, ou s'agit-il toujours des mêmes plats regroupés différemment selon les restaurants ?

<br>
<hr>
<br>

## Question 8 : Les restaurants proposent-ils des menus équilibrés en catégories ?

Les restaurants CROUS proposent-ils un nombre similaire de plats dans chaque catégorie, ou certains établissements sont-ils structurellement déséquilibrés ? On s'attend à une forte hétérogénéité : selon leur format (cafétéria, snack, brasserie), certains restaurants pourraient surreprésenter une catégorie — par exemple beaucoup de Snacking et peu de Plats chauds — tandis que d'autres proposeraient une offre plus équilibrée entre toutes les catégories.

```{r q7-boxplot, fig.cap="Distribution du nombre de plats par catégorie et par restaurant", echo=FALSE, message=FALSE, warning=FALSE, fig.width=16, fig.height=9, out.width="100%"}
nb_plats_cat <- menus %>%
  mutate(categorie_groupe = map_chr(categorie, classer)) %>%
  filter(categorie_groupe != "Autre") %>%
  group_by(restaurant_id, categorie_groupe) %>%
  summarise(nb_plats = n(), .groups = "drop")

ggplot(nb_plats_cat,
       aes(x = categorie_groupe, y = nb_plats, fill = categorie_groupe)) +
  geom_boxplot(outlier.size = 0.8, alpha = 0.7, show.legend = FALSE) +
  scale_fill_brewer(palette = "Set2") +
  coord_flip() +
  labs(
    title = "Nombre de plats par catégorie et par restaurant",
    x     = "Catégorie",
    y     = "Nombre de plats"
  ) +
  theme_minimal(base_size = 11)
```

#### **Interprétation**

Le graphique confirme une forte hétérogénéité entre les catégories et entre les restaurants.

-   **Les Boissons** présentent la plus grande variance — la boîte est très étalée, ce qui signifie que certains restaurants proposent énormément de boissons tandis que d'autres très peu. Les outliers dépassent 1000 plats pour le Snacking et \~900 pour les Formules/Menus — ce sont probablement des restaurants de type cafétéria ou snack spécialisés.

-   **Les Entrées et les Plats** ont des boîtes très resserrées et des médianes basses — la majorité des restaurants proposent peu de plats dans ces catégories, ce qui infirme l'idée d'un menu traditionnel équilibré Entrée/Plat/Dessert.

-   **Le Snacking** a la boîte la plus large avec les outliers les plus extrêmes (\~1400) — certains restaurants sont clairement spécialisés dans ce format.

En résumé, les menus CROUS ne sont **pas équilibrés** entre les catégories : l'offre est dominée par les Boissons et le Snacking, au détriment des Entrées et Plats traditionnels.

**Nouvelle question :** Ces restaurants très spécialisés en Snacking ou Boissons correspondent-ils à un type particulier dans `type.libelle` ? On pourrait croiser ces outliers avec les métadonnées de `liste_restaurants.csv` pour identifier leur profil.


<br>
<hr>
<br>

## Question 14 : Peut-on identifier des restaurants « écoresponsables » ?

###  Formulation de la question

L’objectif de cette analyse est d’identifier si certains restaurants universitaires peuvent être considérés comme **plus écoresponsables que d’autres**, en se basant sur la composition de leurs menus.

Plus précisément, on cherche à répondre à la question suivante :

*Certains restaurants proposent-ils une proportion significativement plus élevée de plats à faible impact carbone, et peut-on les identifier clairement ?*



### Choix méthodologique et indicateur retenu

#### Indicateur utilisé

Chaque plat est associé à une variable qualitative `impact_carbone` (bas, moyen, élevé).
Nous construisons donc, pour chaque restaurant, l’indicateur suivant :

\[
\text{Part de plats à faible impact carbone}
=
\frac{\text{Nombre de plats à impact carbone bas}}
{\text{Nombre total de plats}}
\]

Cet indicateur permet :

* une **comparaison équitable entre restaurants**, indépendamment de leur taille,
* une **lecture simple et interprétable**,
* une approche cohérente avec les enjeux environnementaux de la restauration collective.

#### Visualisations retenues

Pour répondre à la question, plusieurs visualisations complémentaires sont utilisées :

* **Bar chart** : comparer les restaurants entre eux
* **Classement (top / bottom)** : identifier les extrêmes
* **Histogramme avec seuil** : analyser la distribution globale
* **Indicateurs agrégés** : quantifier les restaurants au-dessus / en dessous d’un objectif

Cette approche multi-niveaux permet d’éviter une conclusion basée sur un seul graphique.



```{r, echo=FALSE, message=FALSE, warning=FALSE}
# Préparation des données

library(tidyverse)

menus <- read_csv("../data/menus_complets_enrichis.csv")

menus_clean <- menus %>%
  filter(!is.na(impact_carbone)) %>%
  mutate(impact_carbone = str_to_lower(impact_carbone))

eco_score <- menus_clean %>%
  group_by(restaurant_id) %>%
  summarise(
    total_plats = n(),
    plats_bas_carbone = sum(impact_carbone == "bas"),
    part_bas_carbone = plats_bas_carbone / total_plats
  )

# Chargement des restaurants
restaurants <- read_csv("../data/liste_restaurants.csv")

# Jointure des scores environnementaux avec les informations des restaurants
eco_score_named <- eco_score %>%
  left_join(restaurants, by = c("restaurant_id" = "code")) %>%
  select(restaurant_id, nom, region.libelle, part_bas_carbone)

# Analyse globale : comparaison entre restaurants

ggplot(eco_score_named,
       aes(x = reorder(nom, part_bas_carbone),
           y = part_bas_carbone)) +
  geom_col(fill = "darkgreen") +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format()) +
  labs(
    title = "Part de plats à faible impact carbone par restaurant",
    x = "Restaurant",
    y = "Part de plats à faible impact carbone"
  ) +
  theme_minimal()
```

### Commentaire

Le graphique présentant la part de plats à faible impact carbone pour l’ensemble des restaurants met en évidence une forte dispersion des valeurs.
Bien que certains restaurants atteignent une proportion très élevée de plats bas carbone, d’autres présentent une offre beaucoup plus limitée.
**La lisibilité du graphique est toutefois réduite par le grand nombre d’établissements**, ce qui justifie l’introduction d’analyses ciblées sur des sous-ensembles de restaurants (classements et seuils).

### Critique
* Trop de restaurants -> **surcharge visuelle**
* Graphique utile pour constater l’hétérogénéité, mais pas pour identifier précisément les établissements
* Nécessité de résumer l’information


###  Focus sur les extrêmes : meilleurs et moins bons restaurants


```{r,echo=FALSE}
# a) Restaurants les plus écoresponsables (Top 20)

top_eco_restaurants <- eco_score_named %>%
  arrange(desc(part_bas_carbone)) %>%
  slice_head(n = 20)

ggplot(top_eco_restaurants,
       aes(x = reorder(nom, part_bas_carbone),
           y = part_bas_carbone)) +
  geom_col(fill = "darkgreen") +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format()) +
  labs(
    title = "Restaurants les plus écoresponsables",
    x = "Restaurant",
    y = "Part de plats à faible impact carbone"
  ) +
  theme_minimal()
```

### Commentaire 

Les restaurants les plus écoresponsables se distinguent par une proportion très élevée de plats à faible impact carbone, de 100 %.

Ces établissements proposent une offre fortement orientée vers des plats peu carbonés, ce qui peut refléter une spécialisation (cafétérias, points de vente légers) ou des choix alimentaires plus végétariens.

### Critique
* Ces résultats ne signifient pas nécessairement une démarche environnementale volontaire
* La structure de l’offre (type de restaurant) influence fortement le score
* Les restaurants très spécialisés peuvent être favorisés


```{r,echo=FALSE}
# b) Restaurants les moins écoresponsables (Bottom 20)

bottom_eco_restaurants <- eco_score_named %>%
  arrange(part_bas_carbone) %>%
  slice_head(n = 20)

ggplot(bottom_eco_restaurants,
       aes(x = reorder(nom, part_bas_carbone),
           y = part_bas_carbone)) +
  geom_col(fill = "firebrick") +
  coord_flip() +
  scale_y_continuous(labels = scales::percent_format()) +
  labs(
    title = "Restaurants les moins écoresponsables",
    subtitle = "Plus faible proportion de plats à faible impact carbone",
    x = "Restaurant",
    y = "Part de plats à impact carbone bas"
  ) +
  theme_minimal()
```

### Commentaire

Les restaurants les moins écoresponsables présentent une proportion nettement plus faible de plats à faible impact carbone.
Toutefois, même parmi ces établissements, la part de plats bas carbone reste rarement nulle, ce qui suggère une présence minimale d’alternatives plus durables dans l’offre globale.

Point important (à souligner)

`Être dans le bas du classement ne signifie pas absence totale d’efforts, mais plutôt une offre dominée par des plats plus carbonés` , souvent liée à :

* une offre majoritairement carnée,
* des menus traditionnels,
* ou des contraintes de restauration collective.


### Analyse par seuil environnemental

Plutôt qu’un classement arbitraire, on introduit un **seuil** permettant d’évaluer si un restaurant atteint un objectif environnemental donné.

```{r,echo=FALSE}
seuil <- 0.90  # 80 %

#  Distribution globale avec seuil

ggplot(eco_score_named, aes(x = part_bas_carbone)) +
  geom_histogram(bins = 20, fill = "grey70", color = "white") +
  geom_vline(xintercept = seuil, color = "red", linetype = "dashed") +
  scale_x_continuous(labels = scales::percent_format()) +
  labs(
    title = "Distribution des restaurants selon leur part de plats bas carbone",
    subtitle = paste("Seuil fixé à", seuil * 100, "%"),
    x = "Part de plats à faible impact carbone",
    y = "Nombre de restaurants"
  ) +
  theme_minimal()

# Nombre de restaurants au-dessus / en dessous du seuil

repartition_seuil <- eco_score_named %>%
  summarise(
    total_restaurants = n(),
    nb_en_dessous = sum(part_bas_carbone < seuil),
    nb_au_dela = sum(part_bas_carbone >= seuil),
    part_au_dela = nb_au_dela / total_restaurants
  )

repartition_seuil
```
### Commentaire

L’histogramme montre une distribution fortement concentrée vers les valeurs élevées de part de plats bas carbone.
Le seuil de 90 % se situe à l’intérieur de la masse principale de la distribution, ce qui explique pourquoi une majorité de restaurants le dépasse malgré le caractère a priori exigeant de ce seuil.

 `Point important :`
Le seuil de 90 %, bien que élevé en apparence, reste atteint par une majorité de restaurants, ce qui montre que :

* l’indicateur utilisé est fortement influencé par la nature des plats proposés,
* certains types de restaurants (cafétérias, points de vente rapides) sont structurellement favorisés.

### Conclusion et limites

Cette analyse montre qu’il est possible d’évaluer l’écoresponsabilité des restaurants universitaires à partir de leurs menus et met en évidence des écarts entre établissements. Toutefois, la forte proportion de plats à faible impact carbone limite la capacité de l’indicateur à réellement les différencier.
Les résultats restent à interpréter avec prudence en raison d’une catégorisation simplifiée, de l’absence de pondération par la fréquence de service et de la non-prise en compte d’autres enjeux environnementaux. 


<br>
<hr>
<br>

## Question 16 : La diversité des plats varie-t-elle selon la région ?

L'offre culinaire des restaurants universitaires est-elle homogène sur tout le territoire, ou certaines régions proposent-elles des menus nettement plus variés ? Cette question est importante pour évaluer les inégalités d'accès à une alimentation diversifiée selon le lieu d'études.

Mesurer la « diversité » n'est pas trivial : un simple comptage de plats distincts favorise mécaniquement les grandes régions qui comptent plus de restaurants. Nous mobilisons donc **trois indicateurs complémentaires**, qui éclairent chacun un aspect différent de la diversité :

1. **Plats distincts par restaurant** — diversité normalisée par la taille de la région.
2. **Entropie de Shannon** — mesure l'équilibre de la distribution des plats au sein de chaque restaurant : une entropie haute signifie que les plats sont variés *et* équilibrés (aucun ne domine massivement).
3. **Composition par style culinaire** — répartition des grandes familles culinaires (Cuisine Traditionnelle, Street Food, Fraîcheur, Plats Sucrés, Soupes) pour aller au-delà du simple comptage et décrire *ce que* les régions proposent.

Les régions avec un seul restaurant (Créteil, Rennes) sont exclues de l'analyse car elles ne permettent pas de généralisation régionale.

```{r q16-setup, echo=FALSE, message=FALSE, warning=FALSE}
library(dplyr)
library(ggplot2)
library(readr)
library(forcats)
library(tidyr)
library(scales)

# ── Chargement ──────────────────────────────────────────────────────────────
restaurants <- read_csv("../data/liste_restaurants.csv", show_col_types = FALSE)
menus       <- read_csv("../data/menus_complets_enrichis.csv", show_col_types = FALSE)

# ── Jointure et filtrage ────────────────────────────────────────────────────
df <- menus %>%
  left_join(
    restaurants %>% select(code, region = "region.libelle"),
    by = c("restaurant_id" = "code")
  ) %>%
  filter(!is.na(region))

# Exclure les régions avec un seul restaurant (non représentatives)
regions_valides <- df %>%
  group_by(region) %>%
  summarise(n_rest = n_distinct(restaurant_id), .groups = "drop") %>%
  filter(n_rest >= 2) %>%          # ≥ 2 restaurants pour être conservée
  pull(region)

df <- df %>% filter(region %in% regions_valides)

# ── Palette commune ─────────────────────────────────────────────────────────
palette_regions <- colorRampPalette(c("#b7dde8", "#1a6fa8"))(length(regions_valides))
```

### Graphique 1 — Nombre de plats distincts par restaurant (diversité normalisée)

Le premier biais à corriger est la taille : Lille avec 22 restaurants aura mécaniquement plus de plats distincts que Corse avec 5. Nous divisons donc le nombre de plats uniques par le nombre de restaurants de la région, pour obtenir un indicateur **indépendant de la taille**.

```{r q16-graph1, echo=FALSE, message=FALSE, warning=FALSE, fig.width=13, fig.height=7, out.width="100%"}
# Métrique : plats distincts par restaurant
diversite_norm <- df %>%
  group_by(region) %>%
  summarise(
    n_plats_distincts = n_distinct(plat),
    n_restaurants     = n_distinct(restaurant_id),
    plats_par_resto   = n_plats_distincts / n_restaurants,
    .groups = "drop"
  ) %>%
  mutate(region = fct_reorder(region, plats_par_resto))

ggplot(diversite_norm, aes(x = plats_par_resto, y = region, fill = plats_par_resto)) +
  geom_col(show.legend = FALSE, width = 0.75) +
  geom_text(
    aes(label = sprintf("%.1f", plats_par_resto)),
    hjust = -0.15, size = 3.2, color = "grey30"
  ) +
  geom_vline(
    xintercept = mean(diversite_norm$plats_par_resto),
    linetype = "dashed", color = "firebrick", linewidth = 0.8
  ) +
  annotate(
    "text", x = mean(diversite_norm$plats_par_resto) + 0.3,
    y = 1.5, label = "Moyenne", color = "firebrick", size = 3.2, hjust = 0
  ) +
  scale_fill_gradient(low = "#b7dde8", high = "#1a6fa8") +
  scale_x_continuous(expand = expansion(mult = c(0, 0.15))) +
  labs(
    title    = "Diversité normalisée : plats distincts par restaurant et par région",
    subtitle = "Corrige le biais de taille — les grandes régions ne sont plus avantagées",
    x        = "Nombre moyen de plats distincts par restaurant",
    y        = NULL
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title         = element_text(face = "bold", size = 14),
    panel.grid.major.y = element_blank()
  )
```

#### Commentaire

Une fois normalisée par le nombre de restaurants, le classement change considérablement par rapport à un simple comptage brut. **Lille** reste en tête (≈ 20 plats distincts par restaurant), suivie de **Strasbourg** et **Toulouse**. À l'inverse, **Nice** et **Montpellier** se retrouvent en bas du classement avec moins de 4 plats distincts par restaurant en moyenne, révélant une offre très répétitive. La ligne pointillée indique la moyenne nationale, permettant d'identifier d'un coup d'œil les régions en-dessous du seuil moyen.

#### Critique

- Cette métrique additionne les plats *sur l'ensemble des restaurants* de la région avant de diviser : si tous les restaurants d'une région proposent exactement les mêmes plats, le score sera élevé mais la diversité réelle sera nulle.
- Elle ne distingue pas la diversité *intra*-restaurant (au sein d'un même restaurant) de la diversité *inter*-restaurants (entre établissements d'une même région).

---

### Graphique 2 — Entropie de Shannon : équilibre de l'offre au sein de chaque restaurant

L'entropie de Shannon mesure à quel point les plats sont **équilibrés** dans un menu : une entropie maximale signifie que chaque plat est proposé aussi souvent que les autres, sans qu'un plat unique monopolise l'offre. Elle vaut 0 si un seul plat est servi en permanence, et augmente avec la variété et l'équilibre.

Nous calculons l'entropie de Shannon pour chaque restaurant, puis nous moyennons par région. Cela donne une vision de la diversité *interne* à chaque établissement.

```{r q16-graph2, echo=FALSE, message=FALSE, warning=FALSE, fig.width=13, fig.height=7, out.width="100%"}
# Entropie de Shannon par restaurant
shannon_entropy <- function(x) {
  counts <- table(x)
  probs  <- counts / sum(counts)
  probs  <- probs[probs > 0]
  -sum(probs * log2(probs))
}

shannon_par_resto <- df %>%
  group_by(region, restaurant_id) %>%
  summarise(shannon = shannon_entropy(plat), .groups = "drop")

shannon_par_region <- shannon_par_resto %>%
  group_by(region) %>%
  summarise(
    shannon_moyen = mean(shannon),
    shannon_sd    = sd(shannon),
    n_rest        = n(),
    .groups = "drop"
  ) %>%
  mutate(
    region  = fct_reorder(region, shannon_moyen),
    # Intervalle de confiance à 95 %
    ic_low  = shannon_moyen - qt(0.975, df = n_rest - 1) * shannon_sd / sqrt(n_rest),
    ic_high = shannon_moyen + qt(0.975, df = n_rest - 1) * shannon_sd / sqrt(n_rest)
  )

ggplot(shannon_par_region,
       aes(x = shannon_moyen, y = region, color = shannon_moyen)) +
  geom_segment(
    aes(x = ic_low, xend = ic_high, yend = region),
    linewidth = 1, alpha = 0.35
  ) +
  geom_point(size = 4, show.legend = FALSE) +
  geom_vline(
    xintercept = mean(shannon_par_region$shannon_moyen),
    linetype = "dashed", color = "firebrick", linewidth = 0.8
  ) +
  annotate(
    "text", x = mean(shannon_par_region$shannon_moyen) + 0.05,
    y = 1.5, label = "Moyenne", color = "firebrick", size = 3.2, hjust = 0
  ) +
  scale_color_gradient(low = "#b7dde8", high = "#1a6fa8") +
  scale_x_continuous(limits = c(0, NA)) +
  labs(
    title    = "Entropie de Shannon moyenne par région",
    subtitle = "Mesure l'équilibre de la distribution des plats au sein de chaque restaurant\nLes segments représentent l'intervalle de confiance à 95 %",
    x        = "Entropie de Shannon moyenne (bits)",
    y        = NULL
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title         = element_text(face = "bold", size = 14),
    panel.grid.major.y = element_blank()
  )
```

#### Commentaire

L'entropie de Shannon donne un classement partiellement différent du précédent. **Corse**, **Strasbourg** et **Lille** arrivent en tête, ce qui signifie que leurs restaurants proposent des menus à la fois variés *et* équilibrés, sans plats dominants répétés à l'excès. **Nice**, **Paris** et **Nancy-Metz** se retrouvent en bas du classement : leurs restaurants tournent autour d'un répertoire de plats très restreint. Les intervalles de confiance révèlent également une forte hétérogénéité intra-régionale (grands segments), notamment pour **Toulouse** et **Paris**, où les restaurants sont très inégaux entre eux.

#### Critique

- L'entropie est sensible au nombre d'observations : un restaurant avec peu de plats enregistrés aura une entropie artificiellement faible.
- Elle ne distingue pas la nature des plats (un restaurant peut avoir une entropie haute en proposant 10 fois la même salade verte déclinée différemment).

---

### Graphique 3 — Composition par style culinaire (que proposent réellement les régions ?)

Les deux métriques précédentes mesurent *combien* il y a de diversité, mais pas *quelle* diversité. Ce troisième graphique décrit la **composition culinaire** de chaque région : quelle part de l'offre est dédiée à la cuisine traditionnelle, au street food, aux plats frais ou sucrés ?

```{r q16-graph3, echo=FALSE, message=FALSE, warning=FALSE, fig.width=14, fig.height=8, out.width="100%"}
# Part de chaque style culinaire par région
styles_region <- df %>%
  group_by(region, style_culinaire) %>%
  summarise(n = n(), .groups = "drop") %>%
  group_by(region) %>%
  mutate(pct = n / sum(n)) %>%
  ungroup()

# Ordonnancement des régions par part de "Cuisine Traditionnelle" (croissant → plus diversifiée)
ordre_regions <- styles_region %>%
  filter(style_culinaire == "Cuisine Traditionnelle") %>%
  arrange(pct) %>%
  pull(region)

palette_styles <- c(
  "Cuisine Traditionnelle" = "#1a6fa8",
  "Street Food"            = "#e07b39",
  "Fraîcheur"              = "#3aaa6e",
  "Plats Sucrés"           = "#e8c63a",
  "Soupes"                 = "#9b59b6"
)

styles_region %>%
  mutate(region = factor(region, levels = ordre_regions)) %>%
  ggplot(aes(x = pct, y = region, fill = style_culinaire)) +
  geom_col(width = 0.8, position = "stack") +
  scale_fill_manual(values = palette_styles, name = "Style culinaire") +
  scale_x_continuous(labels = percent_format(accuracy = 1),
                     expand = expansion(mult = c(0, 0.01))) +
  labs(
    title    = "Composition culinaire des menus par région",
    subtitle = "Régions triées par part croissante de Cuisine Traditionnelle → les plus diversifiées en haut",
    x        = "Part dans l'offre totale de la région",
    y        = NULL
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title         = element_text(face = "bold", size = 14),
    panel.grid.major.y = element_blank(),
    legend.position    = "bottom"
  )
```

#### Commentaire

Ce graphique révèle que **la Cuisine Traditionnelle domine massivement l'offre dans toutes les régions**, représentant entre 70 % et 100 % des plats selon les territoires. Néanmoins, des nuances existent : **Bordeaux**, **Strasbourg** et **Amiens** affichent une proportion plus élevée de Street Food et de plats Fraîcheur, signe d'une offre plus contemporaine. À l'inverse, des régions comme **Nice**, **Montpellier** ou **Nancy-Metz** sont presque exclusivement ancrées dans la cuisine traditionnelle, ce qui confirme leur faible score d'entropie observé dans le graphique 2.

#### Critique

- La variable `style_culinaire` ne compte que 5 valeurs, ce qui limite fortement la granularité de l'analyse.
- La prédominance de la Cuisine Traditionnelle peut refléter un biais d'encodage lors de l'enrichissement des données plutôt qu'une réalité culinaire.

---

### Synthèse

| Indicateur | Classement « plus diversifié » | Classement « moins diversifié » |
|---|---|---|
| Plats distincts / restaurant | Lille, Strasbourg, Toulouse | Nice, Montpellier, Paris |
| Entropie de Shannon | Corse, Strasbourg, Lille | Nice, Nancy-Metz, Paris |
| Diversité stylistique | Bordeaux, Strasbourg, Amiens | Nice, Montpellier, Nancy-Metz |

Les trois approches convergent sur plusieurs points : **Nice** et **Montpellier** apparaissent systématiquement comme les régions les moins diversifiées, tandis que **Strasbourg** et **Lille** figurent régulièrement parmi les plus riches. Cette convergence renforce la robustesse du diagnostic malgré les différentes limites propres à chaque métrique.

**Limites générales de l'analyse :**

- Les données couvrent une période courte (mars–mai 2026) et ne reflètent pas nécessairement l'offre annuelle habituelle.
- Le nombre de restaurants par région est très variable (de 5 à 33), ce qui fragilise les comparaisons pour les petites régions comme la Corse ou Antilles-Guyane.
- La variable `plat` contient de nombreuses variantes orthographiques d'un même plat, ce qui peut gonfler artificiellement le comptage de plats distincts.

## Question 22 : Certains restaurants ont-ils des données moins fiables ?

L'enrichissement des données nutritionnelles repose en partie sur une attribution manuelle ou automatisée des Nutri-Scores. Il est légitime de se demander si certains restaurants présentent des **profils nutritionnels suspects**, trahissant une attribution peu différenciée (par exemple, une assignation systématique du score C à tous les plats).

Les données utilisées sont : `restaurant_id` (identifiant nominal), `nutriscore` (catégorielle ordinale A–E), et `plat` (nominale pour le volume).

#### **Méthodologie et nettoyage**

Pour chaque restaurant, nous calculons :

- Le **nombre total de plats** dans les données.
- La **proportion de plats avec le Nutri-Score C**, qui est le score modal dans le dataset (≈72 % des plats). Un restaurant présentant 100 % de C sur un volume significatif de plats est un signal d'alerte fort.
- Le **nombre de Nutri-Scores distincts utilisés** (diversité du scoring) : un restaurant fiable devrait présenter au moins 2 ou 3 valeurs différentes.

Nous filtrons sur les restaurants ayant **au moins 10 plats**, pour éviter que les petits volumes fassent apparaître artificiellement des proportions extrêmes.

#### **Visualisation et analyse**

```{r q22-fiabilite-restaurants, echo=FALSE, message=FALSE, warning=FALSE, fig.width=13, fig.height=7, out.width="100%"}
library(dplyr)
library(ggplot2)
library(readr)
library(scales)

menus      <- read_csv("../data/menus_complets_enrichis.csv", show_col_types = FALSE)
restaurants <- read_csv("../data/liste_restaurants.csv",       show_col_types = FALSE)

# Statistiques de fiabilité par restaurant
fiabilite <- menus %>%
  group_by(restaurant_id) %>%
  summarise(
    nb_plats           = n(),
    nb_scores_distincts = n_distinct(nutriscore),
    pct_C              = mean(nutriscore == "C") * 100,
    .groups            = "drop"
  ) %>%
  filter(nb_plats >= 10) %>%
  left_join(
    restaurants %>% select(code, nom),
    by = c("restaurant_id" = "code")
  ) %>%
  mutate(
    suspect = pct_C >= 90 & nb_scores_distincts == 1,
    label   = if_else(suspect & nb_plats >= 80, nom, NA_character_)
  )

# Graphique : scatter pct_C vs nb_scores_distincts, taille = nb_plats
ggplot(fiabilite,
       aes(x = nb_scores_distincts, y = pct_C,
           size = nb_plats, color = suspect)) +
  geom_jitter(alpha = 0.65, width = 0.15, height = 0.5) +
  geom_hline(yintercept = 90, linetype = "dashed", color = "firebrick", linewidth = 0.8) +
  ggrepel::geom_label_repel(
    aes(label = label),
    size = 2.8, color = "firebrick", fill = "white",
    box.padding = 0.4, max.overlaps = 12, show.legend = FALSE
  ) +
  scale_color_manual(
    values = c("FALSE" = "#7ab8d4", "TRUE" = "firebrick"),
    labels = c("FALSE" = "Données cohérentes", "TRUE" = "Suspect (≥90 % de C, 1 seul score)")
  ) +
  scale_size_continuous(range = c(2, 10), breaks = c(10, 50, 100, 500)) +
  scale_y_continuous(labels = function(x) paste0(x, " %")) +
  labs(
    title    = "Fiabilité des données nutritionnelles par restaurant",
    subtitle = paste0(
      "Restaurants avec ≥ 10 plats — ",
      sum(fiabilite$suspect), " sur ", nrow(fiabilite),
      " présentent un profil suspect (≥ 90 % de Nutri-Score C, 1 valeur unique)"
    ),
    x        = "Nombre de Nutri-Scores distincts utilisés",
    y        = "Proportion de plats avec Nutri-Score C",
    color    = NULL,
    size     = "Nombre de plats"
  ) +
  theme_minimal(base_size = 12) +
  theme(
    plot.title     = element_text(face = "bold", size = 14),
    legend.position = "bottom"
  )
```

```{r q22-top-suspects, echo=FALSE, message=FALSE, warning=FALSE}
# Table des restaurants les plus suspects
fiabilite %>%
  filter(suspect) %>%
  arrange(desc(nb_plats)) %>%
  slice_head(n = 15) %>%
  select(
    `Restaurant`             = nom,
    `ID`                     = restaurant_id,
    `Nb plats`               = nb_plats,
    `% Nutri-Score C`        = pct_C,
    `Scores distincts`       = nb_scores_distincts
  ) %>%
  knitr::kable(
    caption = "Top 15 des restaurants au profil nutritionnel suspect",
    digits  = 1
  )
```

### Commentaire

Sur les `r nrow(fiabilite)` restaurants disposant d'au moins 10 plats, **`r sum(fiabilite$suspect)` présentent un profil suspect** : 100 % de leurs plats se voient attribuer le Nutri-Score C, sans aucune variation. Ce phénomène est particulièrement visible parmi les restaurants avec beaucoup d'entrées (112 plats), suggérant une attribution automatique ou par défaut plutôt qu'un scoring individualisé.

La ligne pointillée rouge à 90 % délimite la zone d'alerte. Les points colorés en rouge (suspects) se concentrent dans le coin supérieur gauche : un seul score distinct et une proportion de C proche de 100 %.

### Critique

- L'absence de valeurs manquantes dans le dataset (`nutriscore` renseigné pour 100 % des lignes) est elle-même un signal inhabituel pour un enrichissement manuel, et renforce le doute sur la fiabilité de certaines attributions.
- Un restaurant ne proposant que des plats réellement classés C n'est pas nécessairement une erreur ; mais la probabilité d'une telle uniformité sur des dizaines de plats différents est très faible.
- Ce diagnostic est exploratoire : il permet d'**identifier des priorités de vérification**, pas de conclure définitivement à une erreur de données.
